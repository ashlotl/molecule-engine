#[derive(Clone)]
pub struct MoleculeApplicationVersion {
    pub major: u32,
    pub minor: u32,
    pub patch: u32,
}

#[derive(Clone)]
pub struct MoleculeVersion {
    pub major: u32,
    pub minor: u32,
    pub patch: u32,
}